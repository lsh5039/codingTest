package codingTest01;

import java.util.HashMap;
import java.util.Map;

import javax.xml.stream.events.EndDocument;

public class Main01 {
	public int solution(int bridge_length, int weight, int[] truck_weights) {
        int answer = 0;//건너는  총 시간
    	
		int wait[];//대기중
		wait = truck_weights; // 처음 대기중
		int ing[] = new int[wait.length];//건너는 중
		int end[] = new int[wait.length];//다 건넘
		
		int time = bridge_length+1;//트럭이 다리를 건너는 시간
		int current=0;//트럭을 가리키는 커서
		time = bridge_length+1;//트럭 하나당 건너는 간시
		
		int truckSum=0;
		
		
		
		while(true) {
			ing[current] = wait[current];
			truckSum +=wait[current];
			truckSum+=
		}
		
		
		
		
			
			
        
        return answer;
    }
	
	public static void main(String[] args) {
		int truck_weights[] = {10};// 문제의 트럭
	
		
		
		Main01 main = new Main01();
		int bridge_length = 100;
		int weight = 100;
		
		int reuslt = main.solution(bridge_length, weight, truck_weights);
		System.out.println("reuslt :: "+reuslt);
	}
	
}
